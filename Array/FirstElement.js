// find first elemen that appear only once 
const array = [1, 8, 9, 0, 7, 5, 21, 2, 3, 4, 5, 6, 7, 8, 9, 78, 90]
function firstElement(array) {
    let set = new Set();

    for (const e of array) {
        let set = new Set()
        for (let i = 0; i < array.length; i++) {
            if (set.has()){

            }
            
            
        }
    }
}
console.log(firstElement(array));


